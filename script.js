const APIUAL = 'https://api.github.com/users/'

const main = document.getElementById('main')
const for0 = document.getElementById('form')
const search = document.getElementById('search')


async function getUser(userName) {
    try {
        const { data }
    }
}